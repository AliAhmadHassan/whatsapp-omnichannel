@model WWeb.UI.WebformMVC.Models.MensagemPredefinida
<div class="content_create">

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary()

        <table style="width:100%;">
            <tr>
                <td>
                    <div class="editor-field">
                        <input type="checkbox" name="cb_bloqueada" /> Bloqueada
                    </div>
                </td>
            </tr>
            <tr>
                <td>

                    <div class="editor-label">
                        @Html.LabelFor(model => model.Mensagem)
                    </div>

                    <div class="editor-field">
                        @Html.TextAreaFor(model => model.Mensagem)
                        @Html.ValidationMessageFor(model => model.Mensagem)
                    </div>
                </td>
            </tr>
        </table>
        <p>
            <input type="submit" value="Cadastrar" />
        </p>
    }
</div>
